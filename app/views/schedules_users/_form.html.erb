<%= form_with(model: schedules_user, local: true) do |form| %>
  <% if schedules_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedules_user.errors.count, "error") %> prohibited this schedules_user from being saved:</h2>

      <ul>
      <% schedules_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
